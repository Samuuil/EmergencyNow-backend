export enum CallStatus {
  PENDING = 'pending',
  DISPATCHED = 'dispatched',
  EN_ROUTE = 'en_route',
  ARRIVED = 'arrived',
  NAVIGATING_TO_HOSPITAL = 'navigating_to_hospital',
  COMPLETED = 'completed',
  CANCELLED = 'cancelled',
}
